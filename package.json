{
    "name": "slack-dead-channels",
    "version": "1.0.3",
    "description": "A Slack bot that returns all channels in a workspace with no new messages in the last n (given) days",
    "main": "src/index.ts",
    "scripts": {
        "start": "ts-node src/index.ts",
        "lint": "prettier -w src/**/*.ts test/**/*.ts",
        "watch": "tsc -w",
        "test": "vitest"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/node": "^24.10.1",
        "prettier": "^3.7.4",
        "typescript": "^5.9.3",
        "vitest": "^4.0.18"
    },
    "dependencies": {
        "@slack/web-api": "^7.13.0",
        "dotenv": "^17.2.3",
        "ts-node": "^10.9.2"
    }
}
